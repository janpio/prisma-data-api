{
  "name": "prisma-data-api",
  "version": "1.0.0",
  "description": "Middleware that intercepts all Prisma queries and translates them into a HTTP request against a long running server that executes the same query against the database and returns the result as the HTTP response, which is then returned for the original query.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/janpio/prisma-data-api.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/janpio/prisma-data-api/issues"
  },
  "homepage": "https://github.com/janpio/prisma-data-api#readme",
  "devDependencies": {
    "prisma": "^2.21.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@prisma/client": "^2.21.2",
    "@types/node": "^14.14.41"
  }
}
